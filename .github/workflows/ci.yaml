name: 'CI'
on:
  pull_request:

jobs:
  api-tests:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Install newman and test reporter
        run: npm install -g newman newman-reporter-junitfull

      - name: Build docker image
        run: docker build --tag first-newman .

      - name: Run project
        run: docker run -d -p 5000:80 first-newman

      - name: Create directory to upload test results
        run: mkdir -p testArtifacts

      - name: Run newman tests
        run: newman run test/newman-testing/first-newman.postman_collection.json -e test/newman-testing/first-newman.postman_environment.json -r junit --reporter-junit-export 'testArtifacts/result.xml' -n 2

      - name: Upload Artifacts
        uses: actions/upload-artifact@v3
        if: success() || failure()
        with:
          name: test-results
          path: testArtifacts/result.xml

      - uses: dorny/test-reporter@v1
        if: always()
        with:
          name: Newman Tests
          path: testArtifacts/*.xml
          reporter: jest-junit
          fail-on-error: true

